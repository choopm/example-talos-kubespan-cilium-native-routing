---
machine:
  network:
    nameservers:
      $patch: delete

---
machine:
  features:
    hostDNS:
      forwardKubeDNSToHost: false # required for cilium native routing

  network:
    nameservers:
      - 8.8.8.8
      - 1.1.1.1
